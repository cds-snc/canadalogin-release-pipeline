schema_version: 2
application: release-pipeline acceptance standard ECS
profile: ecs-service
environments: [dev]

backend:
  dockerfile: acceptance/tests/standard-ecs/app/Dockerfile
  build_args:
    APP_VERSION: "{release_version}"
