schema_version: 2
application: release-pipeline acceptance failure mode
profile: ecs-service
environments: [dev]

backend:
  dockerfile: acceptance/tests/docker-build-failure/app/Dockerfile
  build_args:
    APP_VERSION: "{release_version}"
